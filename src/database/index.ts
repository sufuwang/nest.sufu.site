import TallyEntity from './tally.entity';
import UserEntity from './user.entity';

// const Entities = [UserEntity];
const Entities = [UserEntity, TallyEntity];
export default Entities;
export { TallyEntity, UserEntity };
